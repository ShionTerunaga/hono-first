import { css } from "hono/css"

const styles = {
    container: css`
        width: 80%;
        margin: 20px auto;
        text-align: center;
        font-family: sans-serif;
    `,
}

export default styles
